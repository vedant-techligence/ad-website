module.exports = {
  BASE_RATE_PER_REPEAT_PER_DAY: 50, // ₹ placeholder — cost per repeat, per day
  PLATFORM_FEE_PERCENT: 10, // ₹ placeholder — platform's cut
  GST_PERCENT: 18, // standard India GST on services
};
